# ============================================
# ~/.claude 本地 git 追踪忽略规则
# 本文件由 claude-lab gitinit 从仓库 gitinit/.gitignore.template 覆盖生成
# 迭代方式：修改本模板 → 重新执行 claude-lab gitinit 同步到 ~/.claude/.gitignore
# ============================================

# settings.json 默认追踪（switch 的覆盖目标，追踪其变化是 gitinit 的初衷）；
# 纯本地不 push 无泄露风险。若未来配置远程推送，先执行：
#   git rm --cached settings.json
# 避免 API Key 永久留在 git 历史（git 历史不可改写）

# ---- 本地覆盖配置（Claude Code 本地覆盖，非 lab 产物，用户自维护）----
settings.local.json
*.secret.*
*.env
.env
**/id_rsa*

# ---- 会话 / 历史 / 动态数据（量大且变化频繁）----
projects/
history.jsonl
history/
sessions/
session-env/
tasks/
todos/
plans/
debug/
file-history/
shell-snapshots/
stats-cache.json
telemetry/
statsig/
cache/
backups/
plugins/
ide/
.last-cleanup
.last-update-result.json
chrome_debug_profile/

# ---- 系统 / 临时文件 ----
.DS_Store
Thumbs.db
*.log
logs/
desktop.init
