# CHANGELOG —— ts-code-guide

## v0.1.1 (2026-08-11)

- 文档文件改名为 `ts-comments-guide.md`（英文名，避免带空格中文路径在 `@` 引用与 shell 命令中的转义问题），同步更新 ts-standards 的引用与软依赖检测命令

## v0.1.0 (2026-08-11)

- 初始导入：`TypeScript 代码注释规范指南.md`（v1.3）从仓库 `docs/` 移入 `dist/docs/`，作为独立 docs 类型模块部署到 `~/.claude/docs/`
