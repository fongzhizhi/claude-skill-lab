# feedback

## 初始创建 (2026-08-12)

- 需求来源：统一旧代码注释规范。旧代码注释不符合已在 rules/ 落地的 TS 注释指南，需要一个工具按指南批量调整，注释可大改（增删改、补录），必要时可调整代码结构；范围默认 git diff 变更文件，也可手动指定函数/文件/目录。
- 决策：采用 Skill 而非 Command（依赖长文档 + 多步骤流程 + 多参数模式）。
- 待实测确认项：四种范围模式的实际体验、结构调整的触发尺度、`@ts-ignore` 建议清单的价值。