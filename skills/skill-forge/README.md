# skill-forge

> 本仓库的元技能 —— 用于创建、维护、迭代所有模块（单模块：skills/agents/commands/rules/hooks/workflows；聚合套件：suites/）。

通过自然语言对话驱动，自动生成模块目录、文档文件（design/CHANGELOG/feedback）及 `dist/` 下的成品文件，并持续记录迭代历史。

**你不需要手动编辑任何文档文件** —— 全部交给 `skill-forge` 在对话中自动完成。
