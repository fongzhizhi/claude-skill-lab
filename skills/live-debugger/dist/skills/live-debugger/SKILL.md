---
name: live-debugger
description: 模拟人工 debugger 流程定位前端 bug：分析代码找疑点 → console.log 埋点 → 等编译 → 打开/刷新页面 → 读取 console 数据 → 闭环迭代直至修复。调用浏览器控制 MCP（chrome-devtools / playwright 等）自动化"埋点-复现-看数据-收敛"循环。当用户描述前端 bug、要求定位/调试/排查/修复问题时使用。
version: 0.1.0
---
# live-debugger

模拟人工 debugger 的排查循环：**看代码找疑点 → 埋点 → 复现 → 看数据 → 收敛**。每轮迭代必须基于上一轮数据缩小范围，直至找到根因、用户无法复现。

## 前置：明确 bug 信息

开始前确认用户已提供：**bug 描述**（现象）、**复现步骤**、**涉及页面 URL**（未提供用默认值）。缺失则先询问，不带着模糊目标空跑。

## 前置：确认浏览器控制 MCP 可用

用 ToolSearch 查找浏览器控制工具（navigate / refresh / 读取 console 等，工具名形如 `mcp__chrome-devtools__navigate`、`mcp__playwright__navigate`）。**以实际找到的工具名为准，不要臆造工具名。**

找不到 → 告知用户需要配置浏览器控制 MCP（如 chrome-devtools MCP），本次降级为"纯代码分析 + 用户手动复现"：由用户手动操作页面、打开 DevTools Console 回贴 `[live-debugger]` 日志。

## 六步主流程

### 步骤 1：分析代码，定位疑点

根据 bug 描述和复现步骤分析代码，找到疑点位置。

- **硬性限额**（AI 没有时间概念，用操作次数换时间约束）：

  | 动作 | 上限 |
  | --- | --- |
  | Read | ≤ 5 次 |
  | Grep | ≤ 10 次 |

- 达到上限仍无法定位 → 直接把**功能入口、数据流转关键位置**（入口函数、状态初始化、数据获取/组装/提交处）作为疑点，进入埋点。
- 一轮排查产出 1~3 个疑点即可，不要贪多；范围大时优先埋数据流转关键节点。

### 步骤 2：埋点 / 修复

在疑点位置插入 console.log 埋点；若已确定根因，则直接添加修复代码。

- **为什么用 console.log**：最快，且用户能主动观察；浏览器 MCP 读取 console 即可取数，无需断点、无需调试协议细节。
- **统一埋点格式**（保证可识别、可过滤、可精确清除）：

  ```js
  console.log('[live-debugger][<context>]', { <关键变量> })
  ```

  - `<context>`：语义化位置名，如 `login-submit`、`order-create/fetch`、`useTable/load`
  - `<关键变量>`：与流程判断、数据状态直接相关的变量（参数、局部量、计算结果），放同一个对象里，一条 log 一处疑点
- **修复策略**：
  - 方案明确（代码直接可判、或上一轮埋点数据已证明）→ 直接添加修复代码
  - 方案有多种且无优劣 → 先继续埋点取证，最终修复时选最稳妥方案并在报告中说明；若方案差异显著必须用户定夺时才询问，**尽量不询问**，完全修复后再确认方案也可

### 步骤 3：编译等待

保存后，输出疑点和埋点状态，等待代码编译结束。

- 先判断项目形态：
  - dev server 热更新（vite / webpack dev 等，保存即生效）→ 跳过等待，直接下一步
  - 需要手动编译 → 找编译输出目录：`package.json` build 脚本、`tsconfig.json` outDir、vite `build.outDir` 等；默认看 `/dist`、`/build`、`/out`、`.next`、`/lib` 之类的目录
- **判定已编译成功**：编译输出目录内文件 mtime 晚于最后一次埋点保存时间；找不到任何编译成功信息 → **等 10s 判定已编译成功**
- 若实际未编译（复现时数据未更新），闭环回到本步时提醒用户先触发编译

### 步骤 4：打开 / 刷新页面

调用浏览器 MCP 打开或刷新页面。

- 第一次打开：按用户提供的 URL；未提供用默认 `http://localhost/editor?cll=debugger`
- 页面已被用户打开过 / MCP 已有活动 tab → 直接刷新即可，不重启浏览器
- 刷新后**告知用户"页面已刷新"**，并列出需要用户执行的操作步骤（如填写表单、点击按钮、上传文件、复现操作序列）
- 等待用户复现 bug，回复确认词（"好了""ok""完成"等）后再进入下一步

### 步骤 5：收集埋点信息

用户确认操作完成后，读取 console 中的埋点信息，分析代码。

- 通过浏览器 MCP 读取页面 console（如 read_console / 相关工具），**过滤 `[live-debugger]` 前缀**，按 context 归类
- 无读取 console 的工具 → 请用户手动打开 DevTools Console，复制 `[live-debugger]` 日志回贴
- 分析要点：流程走到了哪一步（在哪中断）、数据是否符合预期（值、类型、时序）

### 步骤 6：闭环

根据埋点数据重新分析代码，回到步骤 1（或直接进入步骤 2 埋新点 / 修复），**每轮疑点范围必须比上一轮小**。用户无法复现此问题后收尾：

1. **清除所有 console 埋点**：按 `[live-debugger]` 前缀 grep 精确删除，不误删业务日志
2. **保留修复代码**，编译产物按项目流程处理
3. **输出报告**：根因、修复方案（改动文件/位置）、验证过程（几轮迭代、每轮数据结论）
4. **关闭浏览器**（MCP 关闭 tab / 浏览器）

## 防御性边界

- **无收敛保护**：连续 3 轮埋点没有产生任何新信息，或用户要求停止 → 停下，输出阶段报告（已确认什么、疑点在哪、建议下一步），询问用户是否继续
- 修复代码必须是**当前证据链支持的改动**，不允许为凑结论而改无关代码
- 清理埋点时若发现修复代码与埋点纠缠（如埋点在修复分支内），先调整代码结构再清理
