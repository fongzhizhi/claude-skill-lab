# CHANGELOG

## v0.1.0（2026-08-12）

- 初始版本：模拟人工 debugger 六步排查流程（分析定位 → 埋点 → 编译 → 页面 → 取数 → 闭环）
- 埋点统一前缀 `[live-debugger][<context>]`，支持精确过滤与清除
- 硬性限额：Read ≤ 5 / Grep ≤ 10；无收敛保护（连续 3 轮无新信息停下报告）
- 依赖浏览器控制 MCP（软依赖，缺失降级为手动复现模式）
