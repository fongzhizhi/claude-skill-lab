# CHANGELOG —— ts-standards

## v0.1.2 (2026-08-13)

- `ts-coding-style` / `ts-types` / `ts-error-handling` 规则引用 `docs/ts-code-guide` 新增的三份详细指南，`meta.json` 同步新增三条软依赖（缺失仅提示不拦截）

## v0.1.1 (2026-08-11)

- `ts-testing` 规则引用 `docs/ts-code-guide` 的 `ts-testing-guide.md` 详细指南，`meta.json` 新增对应软依赖（缺失仅提示不拦截）

## v0.1.0 (2026-08-11)

- 初始发布：5 条 TS 规则（ts-comments / ts-coding-style / ts-types / ts-error-handling / ts-testing）
- `ts-comments` 引用 `docs/ts-code-guide` 指南文档，`meta.json` 声明软依赖（缺失仅提示不拦截）
